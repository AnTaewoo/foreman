# sample-app

Tiny Flask app used as the foreman fixture repo. It exposes a `/users` in-memory store.

## Layout
- `src/app/main.py` — Flask app factory and routes
- `src/app/models.py` — dataclasses
- `tests/` — pytest

## Conventions
- ruff, 100 cols
