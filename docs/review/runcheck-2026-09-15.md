# 재실행 점검 리포트 — 2026-09-15 (어제 리포트 F-1~F-10 대비, 수정 없음)

그 사이 들어온 커밋: P7.1 수정(`aa6825a`), PC-7 문서(`f3159c1`)뿐. 실행 파일 변경은 `docs/runbook.md` 1줄. 즉 버그 수정 커밋은 없었고, 아래는 같은 절차를 다시 돌린 결과다.

## 결과 요약

| 단계 | 어제 | 오늘 |
|---|---|---|
| `make migrate` | OK | OK (`0002 (head)`) |
| `scripts/check_runbook.sh` | 5/5 | 5/5 (fake e2e PASS) |
| `make run-api` + 프로젝트→Goal→Plan | reload로 고착 | OK. 리로드 0회(동시 편집 없음), Goal 5초, Plan 30초 |
| `/approve` 웹훅 | 빈 시크릿 서명 통과 | `.env`에 시크릿이 생겨 빈 키 서명은 401, 실제 키로 202 |
| Task → Docker 워커 (repo가 `HITL_REPO_ROOT` 밖) | 2초 만에 죽고 `assigned` 고착 | **재현** (컨테이너 2개, 5분간 `assigned` 2) |
| Task → Docker 워커 (repo가 `./repos` 아래) | 미시도 | 워커 실행됨(31초, tool 16회) → `git push` 권한 오류로 exit 1 → `running` 고착 |
| `make check` | 통과 | 통과 (457 passed, mypy OK) |

## 상태 변화

- **F-1 (org/pc1 clone 루프)**: 오늘 기동에서는 재현 안 됨(clone 시도 0회). 어제 루프는 Redis `:retry` 큐에 남아 있던 항목이 소진되면서 끝난 것으로 보임. 근본 원인(dry 모드에서 실 clone, 백오프 없음)은 그대로.
- **F-2 (`done -> ready/assigned` projection 재시도)**: 그대로. 기동 직후 72개 event_id가 attempt=5로 재시도(재시도 큐가 재기동 간 영속).
- **F-3 (`events:P1` FK 위반 무한 재시도)**: 그대로. attempt=37로 카운터가 이어짐. 상한 없음 확인.
- **F-4 (`--reload`)**: 오늘은 파일 변경이 없어 발생 안 함. 조건부 재현이므로 그대로 유효.
- **F-5 (워커 즉시 사망, 미감지)**: 재현 + 원인 분해 완료 → 아래 F-5a/F-5b/F-5c.
- **F-6 (repo 중복 라우팅)**, **F-7 (read-after-write)**: 오늘은 control plane이 한가해 지연 5초. 그대로 유효.
- **F-8 (빈 시크릿)**: `.env`에 시크릿이 설정돼 현재 환경에서는 닫힘. 코드 차원의 fail-closed는 미확인(설정이 비면 여전히 통과할 것).
- **어제 고착된 runcheck3 Task**: 14시간째 `assigned`. 재기동해도 재배정·실패 처리 없음.

## 새로 확정된 원인 (수정 대상)

### F-5a [High] 로컬 경로 프로젝트가 `HITL_REPO_ROOT` 밖에 있으면 워커가 즉시 죽고 아무 이벤트도 없음
- repo=`/home/lhjin0j/.claude/jobs/8428db5e/tmp/repo4` (프로젝트 4, `01M2H6N65SHKB93TMM5M5YNDGG`): `launcher.started` 2건(`acec957d695e`, `52d7ed90fece`) 후 2초 내 삭제. 스트림에 `task.assigned`까지만 있고 `task.started` 없음.
- repo=`/home/lhjin0j/foreman/repos/runcheck5` (프로젝트 5, `01M2H7AQEQFGA7TAC2NSJZKCE4`): 워커가 정상 기동해 코딩까지 진행.
- → 런처가 `HITL_REPO_ROOT`만 컨테이너에 마운트하므로 그 밖의 로컬 경로는 컨테이너 안에서 존재하지 않아 기동 실패. runbook의 "로컬 경로 프로젝트는 그대로"는 docker 런처에서는 성립하지 않는다.
- 고칠 곳 후보: DockerCliLauncher 마운트(프로젝트 repo 경로를 개별 마운트) 또는 API에서 REPO_ROOT 밖 경로 거부 + runbook 수정.

### F-5b [High] 컨테이너 uid(10001 `worker`) ≠ 호스트 repo 소유자 uid(1003, 755) → `git push` 거부 → run 실패 exit 1
- docker events: `create/start` 00:27:40 → `die exit=1` 00:28:14 → `destroy` (컨테이너 이름 `foreman-worker-01m2h7cpsdzqvyxkd2bhdabc98`).
- `run.tool_denied` / `run.finished{agent_outcome: failed}` payload:
  ```
  git push -q -u origin ai/adding-users-api/1-add-get-users-endpoint: error: remote unpack failed: unable to create temporary object directory
  To /home/lhjin0j/foreman/repos/runcheck5
   ! [remote rejected] ai/adding-users-api/1-add-get-users-endpoint -> ...
  ```
- `repos/runcheck5`에 `ai/*` 브랜치 없음(push 실패 확인). `tests/…/test_docker_launcher_starts_wo0`가 통과하는 것은 테스트가 tmp 디렉터리에 다른 권한으로 만들기 때문으로 추정(어제 pytest 경고의 root 소유 잔여 파일과 같은 계열).
- 고칠 곳 후보: `worker/Dockerfile`의 고정 uid 10001 → `docker run --user $(id -u):$(id -g)` 또는 마운트 권한 정책; 문서에 명시.

### F-5c [High] `run.finished(failed)` 뒤에 `task.failed`가 없어 Task가 `running`에 영구 고착, Scheduler `in_flight`도 안 줄어듦
- 프로젝트 5 Task `01M2H7CP86G4HEAHJ47PK95N9B`: DB `running`, 4분 이상 변화 없음. 스트림/DB 모두 `task.failed`/`task.completed` 없음.
- 프로젝트 4의 죽은 컨테이너 2개가 그대로 in_flight에 남아 프로젝트 5 배정 시 `in_flight=3`. `HITL_SCHEDULER_MAX_WORKERS=4`이므로 죽은 워커가 하나만 더 생기면 전체 스케줄링이 멈춘다.
- 고칠 곳 후보: 워커가 실패 시 `task.failed`를 발행하거나, 런처가 컨테이너 exit code를 보고 `task.failed`를 발행; Scheduler가 `run.finished(failed)`/컨테이너 종료에 in_flight를 감소; stuck 타임아웃.

### F-11 [Medium] 워커 이벤트가 Redis 스트림에 두 번씩 발행됨
- `events:<p5>` 34 entries 중 distinct id 30. 중복 4건: `task.started`, `run.started`, `run.tool_denied`, `run.finished` (같은 id, 같은 ts). projection은 id로 중복 제거해 DB에는 1건씩만 들어감.
- 고칠 곳 후보: 워커 publish 경로(파일 publish + 직접 XADD 이중 발행?) — `python -m worker --publish-file` 옵션과 관련 추정.

### F-12 [Low] `run.tool_called` 16건이 스트림에는 있으나 DB `events`에는 0건
- projection이 `run.tool_called`를 저장하지 않는 것이 설계라면 문제 없음. 설계가 "모든 이벤트 append-only 저장"이면 누락.

## 남긴 상태
- Docker 스택은 켜 둠. 오늘 띄운 control plane/API(8000)는 종료.
- DB에 프로젝트 `runcheck4`(01M2H6N65SHKB93TMM5M5YNDGG, `assigned` 2 고착), `runcheck5`(01M2H7AQEQFGA7TAC2NSJZKCE4, `running` 1 고착) 추가. 어제 것(runcheck/2/3)도 그대로.
- `repos/`는 gitignore 대상이 아니라 `repos/runcheck5/`가 untracked로 잡혀 삭제함(재현하려면 `./repos` 아래에 git repo 사본을 두고 프로젝트를 만들면 됨).
- 로그: `/home/lhjin0j/.claude/jobs/8428db5e/tmp/r2-*.log`, 스트림 덤프 `r2-stream5.json`.
- 작업 트리 변경 없음.
